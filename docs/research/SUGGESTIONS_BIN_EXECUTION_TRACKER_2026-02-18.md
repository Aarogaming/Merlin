# Suggestions Execution Tracker (2026-02-18)

Source priority matrix: `docs/research/SUGGESTIONS_BIN_PRIORITY_MATRIX_2026-02-18.json`
Status key: `pending` -> `in_progress` -> `done`

## Immediate Tier (Top 15)

| ID | Status | Owner | Notes | Summary |
| --- | --- | --- | --- | --- |
| `S001` | done | merlin-agent | Added centralized `_validate_operation_envelope` pre-dispatch validation gate in `execute_operation`. | Add centralized operation-envelope request validation middleware to remove repeated schema checks (targets: `merlin_api_server.py`, `contracts/aas.operation-envelope.v1.schema.json`). |
| `S003` | done | merlin-agent | Added global/per-operation payload-byte limits with `PAYLOAD_TOO_LARGE` enforcement and env controls. | Enforce bounded payload-size limits per operation to prevent oversized body abuse (targets: `merlin_api_server.py`, `merlin_settings.py`). |
| `S006` | done | merlin-agent | Added `/merlin/operations/metrics` with per-operation count/error rate and p50/p95/p99 latency telemetry. | Add per-operation latency percentiles (p50/p95/p99) in API status output (targets: `merlin_api_server.py`, `merlin_metrics_dashboard.py`). |
| `S007` | done | merlin-agent | Added error category taxonomy (`validation/auth/policy/dependency/unknown`) into operation error payloads. | Split error taxonomy into transport, validation, auth, dependency, and policy classes (targets: `merlin_api_server.py`, `tests/test_operation_error_responses.py`). |
| `S014` | done | merlin-agent | Added configurable per-operation 60s rate limiter with retryable `RATE_LIMITED` responses. | Add per-operation rate limiting with clear retry hints (targets: `merlin_api_server.py`, `merlin_policy.py`). |
| `S020` | done | merlin-agent | Added `scripts/run_operation_conformance_suite.py` + dry-run tests for operation fixture conformance suites. | Add endpoint conformance runner that validates request/response against fixture contracts (targets: `tests/test_operation_expected_responses.py`, `tests/fixtures/contracts`). |
| `S024` | done | merlin-agent | Replaced raw fallback strings with `RoutingFallbackReasonCode` enum + shared helper functions and tests. | Move fallback reason strings to a shared enum module to prevent drift (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`). |
| `S025` | done | merlin-agent | Moved common prompt bucket and DMS preference policy into `merlin_routing_contract.py` and switched all three routers to shared helpers. | Centralize route-policy logic into one helper used by adaptive/parallel/streaming backends (targets: `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `merlin_streaming_llm.py`). |
| `S026` | done | merlin-agent | Added shared deterministic A/B helper (`select_dms_ab_variant`) and wired all routers to pass stable query-based assignment keys. | Make A/B assignment deterministic per conversation or correlation hash (targets: `merlin_ab_testing.py`, `merlin_adaptive_llm.py`). |
| `S027` | done | merlin-agent | Added explicit SSE payload parser (`_iter_sse_payloads`) handling comments, heartbeat lines, multiline `data:` frames, and `[DONE]` termination; expanded streaming parser tests. | Build explicit SSE parser state machine to handle comments/heartbeat/[DONE] frames (targets: `merlin_streaming_llm.py`, `tests/test_merlin_streaming_llm.py`). |
| `S033` | done | merlin-agent | Added routing-mode early cancellation for pending parallel futures once preferred route resolves; exposed cancellation metadata and tests. | Add early-cancel for losing branches in parallel model execution (targets: `merlin_parallel_llm.py`, `tests/test_merlin_parallel_llm.py`). |
| `S037` | done | merlin-agent | Added adaptive DMS rolling error-budget policy with cooldown auto-reenable, settings knobs, status metadata, and targeted tests. | Add automatic DMS disable/enable policy based on rolling error budget (targets: `merlin_adaptive_llm.py`, `merlin_settings.py`). |
| `S041` | done | merlin-agent | Added TTL-driven archival policy (`archive/` store), session auto-archiving on read/list, archive-status protections for mutation, and settings/env controls with tests. | Add session TTL and archival policy for old research-manager sessions (targets: `merlin_research_manager.py`, `merlin_settings.py`). |
| `S065` | done | merlin-agent | Added packaged-plugin manifest validation gate (schema/name/version/entry/capability checks) before module load, with traversal protection and plugin-manager tests. | Enforce plugin manifest schema validation before plugin load (targets: `merlin_plugin_manager.py`, `plugins/*/manifest.json`). |
| `S093` | done | merlin-agent | Added CI critical-module coverage gate (`pytest-cov`) for API/routing/research-manager modules with enforced threshold and pytest marker registration. | Add coverage gate for critical modules (API, routing, research manager) (targets: `pytest.ini`, `.github/workflows/ci.yml`). |

## Near-Term Tier (Next 35)

| ID | Status | Owner | Notes | Summary |
| --- | --- | --- | --- | --- |
| `S002` | done | merlin-agent | Added envelope schema-version negotiation with explicit `SCHEMA_VERSION_DOWNGRADE_REQUIRED`/`SCHEMA_VERSION_UPGRADE_REQUIRED` errors and semver validation; updated protocol doc and API tests. | Add schema-version negotiation and explicit downgrade errors for incompatible envelopes (targets: `merlin_api_server.py`, `docs/protocols/operation-envelope-v1.md`). |
| `S004` | done | merlin-agent | Added mutating-operation correlation gate with deterministic `MISSING_CORRELATION_ID` validation responses and coverage for mutating/read operation behavior. | Require `correlation_id` for all mutating operations and return deterministic validation errors when absent (targets: `merlin_api_server.py`, `tests/test_merlin_api_server.py`). |
| `S005` | done | merlin-agent | Added idempotency-key validation for mutating operations, bounded replay cache keyed by operation+idempotency key, replay header support (`X-Merlin-Idempotent-Replay`), and test isolation reset for cache/rate windows; verified with targeted API/error suites. | Add optional `Idempotency-Key` handling for safe retry of create/update style operations (targets: `merlin_api_server.py`, `docs/protocols/operation-envelope-v1.md`). |
| `S008` | done | merlin-agent | Added startup fail-fast contract-schema self-check (`_validate_required_contract_schemas`) for required operation/manifest/routing schemas; added pass/fail tests in API suite. | Add startup contract self-check that fails fast when required schemas are missing (targets: `merlin_api_server.py`, `scripts/sync_contract_schemas.py`). |
| `S009` | done | merlin-agent | Added `GET /merlin/operations/capability-flags` with active flag records (`name`, `value`, `source`, `details`) and env/default/runtime source attribution; updated capability protocol doc and API tests. | Add endpoint to expose active capability flags with source (env/default/runtime) for debugging (targets: `merlin_api_server.py`, `docs/protocols/repo-capabilities-merlin-v1.md`). |
| `S010` | done | merlin-agent | Added deprecation-policy map for planned replacement operations and automatic response headers (`Deprecation`, `Sunset`, `X-Merlin-Replacement-Operation`, `Link`) across live and idempotent-replay responses; expanded API tests and compatibility-policy doc. | Add API deprecation headers for operations planned for replacement (targets: `merlin_api_server.py`, `docs/protocols/compatibility-policy.md`). |
| `S011` | done | merlin-agent | Added `GET /merlin/operations/spec` OpenAPI-like snapshot (`AAS.OperationSpecSnapshot`) with operation catalog, deprecation metadata, request-schema pointers, and protocol doc links; added API tests and protocol README references. | Add OpenAPI-like exported spec snapshot for public Merlin operations (targets: `merlin_api_server.py`, `docs/protocols/README.md`). |
| `S012` | done | merlin-agent | Added debug-gated `GET /merlin/operations/replay-diagnostics` (`AAS.OperationReplayDiagnostics`) with masked idempotency-cache rows and TTL stats; introduced `MERLIN_OPERATION_REPLAY_DIAGNOSTICS_ENABLED` setting, env example, protocol notes, and API/settings tests. | Add operation replay diagnostics endpoint gated to local debug mode (targets: `merlin_api_server.py`, `merlin_settings.py`). |
| `S013` | done | merlin-agent | Added strict API key rotation flow with multi-key support (`MERLIN_API_KEYS`), optional hot-reload file (`MERLIN_API_KEY_ROTATION_FILE`), strict fallback behavior (`MERLIN_API_KEY_ROTATION_STRICT`), and parser/validation tests in auth + API suites. | Add strict auth key rotation support with hot reload (targets: `merlin_auth.py`, `merlin_api_server.py`). |
| `S015` | done | merlin-agent | Added request-audit metadata contract builder in `merlin_audit.py` and middleware emission in operation dispatch (`request_id`, `route`, `decision_version`, `operation_name`, `status_code`); documented contract and added audit/API tests. | Add request audit metadata contract (`request_id`, `route`, `decision_version`) (targets: `merlin_api_server.py`, `merlin_audit.py`). |
| `S016` | done | merlin-agent | Added endpoint dependency circuit breaker with per-operation mapping, open-state short-circuit (`DEPENDENCY_CIRCUIT_OPEN`), settings/env knobs, and targeted API/self-healing coverage. | Add endpoint-level circuit breaker integration for unstable dependencies (targets: `merlin_api_server.py`, `merlin_self_healing.py`). |
| `S017` | done | merlin-agent | Upgraded JSON formatter to persist structured extra fields and added middleware access-log events (`http_access`) with request metadata plus prompt/content redaction in operation payload previews; added logger + API redaction tests. | Add structured access logs with redaction of prompt/content fields (targets: `merlin_logger.py`, `merlin_api_server.py`). |
| `S018` | done | merlin-agent | Added explicit HTTP runtime tuning settings (`MERLIN_HTTP_KEEP_ALIVE_TIMEOUT_S`, `MERLIN_HTTP_GRACEFUL_SHUTDOWN_TIMEOUT_S`, `MERLIN_HTTP_LIMIT_CONCURRENCY`, `MERLIN_API_RELOAD`) and wired uvicorn startup kwargs through `_uvicorn_runtime_kwargs()`; added env examples and API/settings tests. | Add HTTP timeout and keep-alive tuning defaults for production-like local loads (targets: `merlin_api_server.py`, `docker-compose.yml`). |
| `S019` | done | merlin-agent | Added operation-level rollout flags via `MERLIN_OPERATION_FEATURE_FLAGS` parser (`operation=enabled|disabled`) and enforcement gate in envelope validation (`OPERATION_DISABLED` policy error); surfaced in capability-flags endpoint, env example, and API/settings tests. | Add operation-level feature flags for safer phased rollouts (targets: `merlin_settings.py`, `merlin_api_server.py`). |
| `S021` | done | merlin-agent | Added optional token-aware prompt bucketing (`MERLIN_PROMPT_BUCKET_TOKEN_AWARE`, `DMS_MIN_PROMPT_TOKENS`) in shared routing contract and wired adaptive/parallel routing decisions (plus streaming parity) with coverage. | Replace character-based prompt buckets with optional token-aware buckets (targets: `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`). |
| `S022` | done | merlin-agent | Added DMS warmup/readiness controls (`DMS_WARMUP_ENABLED`, timeout/prompt knobs), cached readiness probe, public readiness accessor, and fallback-to-LM behavior with warmup tests. | Add DMS cold-start warmup request and expose readiness state (targets: `merlin_llm_backends.py`, `merlin_settings.py`). |
| `S023` | done | merlin-agent | Added per-backend timeout profile matrix (`MERLIN_MODEL_TIMEOUT_*`, `MERLIN_MODEL_TIMEOUT_MATRIX`) and prompt-bucket timeout resolution in `LLMBackend` with explicit-timeout override tests. | Add per-model timeout matrix (`short`, `medium`, `long`) (targets: `merlin_settings.py`, `merlin_llm_backends.py`). |
| `S028` | done | merlin-agent | Added streaming TTFT/completion telemetry capture (`stream_ttft_seconds`, `stream_completion_seconds`) and aggregate stream-latency status metrics, then surfaced stream latency summary in metrics dashboard output with tests. | Capture time-to-first-token and stream completion latency for streamed routes (targets: `merlin_streaming_llm.py`, `merlin_metrics_dashboard.py`). |
| `S029` | done | merlin-agent | Added pluggable quality-scoring hook interface (`register/clear/score_response_quality_with_hook`) in `merlin_quality_gates.py`, integrated adaptive scoring path to consume hook output (`quality_hook_applied/source/score/error` metadata), and added quality-gates + adaptive tests validating override behavior and telemetry updates. | Add quality-scoring hook interface to compare DMS vs control responses (targets: `merlin_adaptive_llm.py`, `merlin_quality_gates.py`). |
| `S030` | done | merlin-agent | Added system-prompt prefix cache controls (`MERLIN_SYSTEM_PROMPT_CACHE_ENABLED`, `MERLIN_SYSTEM_PROMPT_CACHE_MAX_ENTRIES`) and cached prompt builder in `LLMBackend`, wired into HuggingFace prompt construction with targeted cache/payload tests. | Add cached-prefix prompt construction helper for repeated system prompts (targets: `merlin_llm_backends.py`, `merlin_settings.py`). |
| `S031` | done | merlin-agent | Added guarded fast short-prompt lane (`MERLIN_ROUTER_FAST_SHORT_*`) in adaptive/parallel backends to execute a single preferred non-DMS model path for very short requests, with metadata stamps and targeted tests. | Add ultra-fast short-prompt lane that bypasses heavy routing checks (targets: `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`). |
| `S032` | done | merlin-agent | Added prompt preflight stage with near-limit/truncation warning codes, message trimming/drop policy, merged response metadata (`prompt_preflight`, `warnings`), env settings, and targeted backend/routing/settings tests. | Add prompt truncation + warning metadata for near-token-limit requests (targets: `merlin_llm_backends.py`, `merlin_routing_contract.py`). |
| `S034` | done | merlin-agent | Added shared retry utility (`RetryBackoffPolicy`, exponential backoff + jitter + retry budget cap) in `merlin_utils.py`; wired optional backend HTTP retries for provider dispatch/warmup with retryable error classification (timeouts, connection errors, 429/5xx), settings/env controls, and targeted utils/backend/settings coverage. | Add shared retry/backoff utility with jitter and retry budget caps (targets: `merlin_utils.py`, `merlin_llm_backends.py`). |
| `S035` | done | merlin-agent | Verified and locked route-policy stamping via shared routing contract defaults (`router_policy_version`, `routing_telemetry_schema`) consumed across adaptive/parallel/streaming backends with existing and re-run test coverage. | Add route policy version stamp in every routing metadata payload (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`). |
| `S036` | done | merlin-agent | Added parameterized router regression corpus suites for adaptive and parallel backends covering short non-DMS selection, long DMS preference, fast short-lane selection, and token-aware DMS selection with expected metadata assertions. | Add router regression corpus with expected model selections (targets: `tests/test_merlin_adaptive_llm.py`, `tests/test_merlin_parallel_llm.py`). |
| `S038` | done | merlin-agent | Added shared usage normalization (`usage_normalized`) across backend responses with support for OpenAI and alt provider fields (`prompt_eval_count`, `eval_count`, `cached_tokens` variants); expanded backend tests for normalized usage coverage. | Normalize usage parsing across OpenAI-compatible providers (`usage`, `cached_tokens`) (targets: `merlin_llm_backends.py`, `tests/test_merlin_llm_backends.py`). |
| `S039` | done | merlin-agent | Added shared provider response normalization helper (`normalize_provider_chat_payload`) with payload-shape stamping and canonical `choices[].message.content` coercion; wired backend normalization path before usage/metadata emission and added routing/backend coverage for alternate provider payload shapes. | Add provider response normalization layer before scoring/metadata emission (targets: `merlin_llm_backends.py`, `merlin_routing_contract.py`). |
| `S040` | done | merlin-agent | Added prompt-safety analyzer (`analyze_prompt_safety`) with high-risk keyword detection and mode-aware blocking in `merlin_policy.py`; integrated adaptive pre-dispatch safety gate that blocks high-risk prompts in safe mode with policy metadata and response path coverage. | Add safety pre-check stage for high-risk prompts before model dispatch (targets: `merlin_policy.py`, `merlin_adaptive_llm.py`). |
| `S042` | done | merlin-agent | Added research-session provenance fields (`created_by`, `source_operation`, `policy_version`) at creation with normalization/migration defaults in `ResearchManager`, wired API + operation-envelope create paths to stamp concrete provenance values, and added targeted manager/API coverage for persisted provenance behavior. | Add provenance fields (`created_by`, `source_operation`, `policy_version`) on research sessions (targets: `merlin_research_manager.py`, `merlin_api_server.py`). |
| `S043` | done | merlin-agent | Added reusable `calibrate_signal_confidence(...)` helper and applied it in `add_signal` (storing both `confidence_raw` and calibrated `confidence`), then added targeted calibration tests and regression checks for research-manager API flows. | Add confidence calibration helper for signal scoring (targets: `merlin_research_manager.py`, `tests/test_merlin_research_manager.py`). |
| `S044` | done | merlin-agent | Added brief template versioning fields (`brief_template_id`, `brief_template_version`) to research-manager brief outputs, updated protocol documentation with the versioning contract, and added API/research-manager tests plus fixture updates for traceability. | Version brief output templates so brief schema changes stay traceable (targets: `merlin_research_manager.py`, `docs/protocols/operation-envelope-v1.md`). |
| `S045` | done | merlin-agent | Added standardized `log_read_only_rejection(...)` audit helper in `merlin_audit.py`, wired research-manager write guards to emit explicit audit entries before read-only `PermissionError`s, and added targeted audit + research-manager read-only tests. | Add explicit audit entries for read-only mode rejections (targets: `merlin_research_manager.py`, `merlin_audit.py`). |
| `S046` | done | merlin-agent | Added `stable_claim_hash(...)` in `merlin_utils.py` and integrated research-manager signal deduplication by claim hash with duplicate metadata (`deduplicated`, `dedup_reason`, `duplicate_count`, `last_duplicate_*`) so repeated noisy claims do not inflate hypothesis scoring; added utils/research-manager/API coverage. | Add signal deduplication by stable claim hash to reduce noisy evidence (targets: `merlin_research_manager.py`, `merlin_utils.py`). |
| `S047` | done | merlin-agent | Added contradictory-signal conflict summarization in research briefs (`contradicting_signal_count`, `conflict_count`, `conflict_hypotheses`) and expanded research-manager/API/fixture tests to validate conflict-count exposure. | Track contradictory signals and expose conflict counts in briefs (targets: `merlin_research_manager.py`, `tests/test_merlin_research_manager.py`). |
| `S053` | done | merlin-agent | Added a dedicated research-manager error-variant fixture pack (`merlin.research.manager.error_variants.cases.json`) and fixture-driven operation tests in `test_operation_expected_responses.py` covering create/list/get/signal/brief validation, read-only, and not-found branches; also updated brief expected-response fixtures for expanded brief fields. | Expand operation contract fixtures for all research-manager success and error variants (targets: `tests/fixtures/contracts`, `tests/test_operation_expected_responses.py`). |

## Backlog Tier (Later 50)

| ID | Status | Owner | Notes | Summary |
| --- | --- | --- | --- | --- |
| `S048` | done | merlin-agent | Added causal evidence-chain generation in research briefs (`causal_chains` with supporting/contradicting evidence links per hypothesis) and introduced CLI `research brief --render causal` rendering mode with targeted research-manager/CLI/API tests. | Add causal chain rendering in brief output for hypothesis evidence links (targets: `merlin_research_manager.py`, `merlin_cli.py`). |
| `S049` | done | merlin-agent | Added research-session snapshot export/import in `ResearchManager` (`AAS.ResearchSessionSnapshot` schema with overwrite controls) and wired CLI commands `research export`/`research import` for JSON snapshot round-trips; added manager/CLI/API regression tests. | Add export/import CLI for research sessions to JSON snapshots (targets: `merlin_cli.py`, `merlin_research_manager.py`). |
| `S050` | done | merlin-agent | Added session tagging support (`tags`) in research session schema, tag/topic filtering in session listing flows, CLI support for tagged create + filtered list (`--tag`, `--topic`), and API/operation payload/query support with validation coverage and fixture updates. | Add session tagging and topic search filters in API/CLI (targets: `merlin_research_manager.py`, `merlin_cli.py`). |
| `S051` | done | merlin-agent | Added `research batch --file <json>` CLI execution mode with step-wise action processing (`create`, `signal`, `brief`, `session`, `list`), `$last_session_id` token resolution, structured batch result envelope, and CLI tests validating repeated-operation execution. | Add batch-mode CLI commands for repeated research operations (targets: `merlin_cli.py`, `tests/test_merlin_cli.py`). |
| `S052` | done | merlin-agent | Added research-session lifecycle event emission in `ResearchManager` (`session.created`, `session.signal_added`, `session.signal_deduplicated`, `session.imported`) with emitter hooks, wired API-server research manager bootstrap to forward events through Hub client emitters when available, and added webhook-capable `emit_research_session_event(...)` support in `merlin_hub_client.py` with dedicated tests. | Add webhook/event emitter for research session updates (targets: `merlin_research_manager.py`, `merlin_hub_client.py`). |
| `S054` | done | merlin-agent | Added cursor-based pagination for research session listings (`list_sessions_page` with `next_cursor`) in `ResearchManager` and integrated cursor support in REST and operation-envelope list endpoints (`payload.cursor` / query `cursor`) with validation and targeted manager/API/fixture test coverage. | Add cursor-based pagination for large research session lists (targets: `merlin_research_manager.py`, `merlin_api_server.py`). |
| `S055` | done | merlin-agent | Added manager-level keyword search (`search_sessions`) and new REST endpoint `/merlin/research/manager/search` with tag/cursor support and validation; expanded research-manager/API tests to cover positive search and invalid-query handling. | Add search endpoint for research sessions by objective keyword (targets: `merlin_api_server.py`, `merlin_research_manager.py`). |
| `S056` | done | merlin-agent | Added optional research brief queue controls in `ResearchManager` (`enqueue_brief_generation`, `process_brief_queue`, job status accessors) with task-manager status integration for queued brief generation workflows; added queue-enabled/disabled behavior tests. | Add optional background summarization queue for expensive brief generation (targets: `merlin_research_manager.py`, `merlin_tasks.py`). |
| `S057` | done | merlin-agent | Completed risk-scoring rubric support end-to-end: `create_session` accepts `impact`, `uncertainty`, `time_horizon`; migration normalizes/backfills rubric on legacy payloads; REST + envelope create paths validate/pass rubric fields; session/get/brief fixtures and targeted manager/API tests updated. | Add risk-scoring rubric fields (`impact`, `uncertainty`, `time_horizon`) in session schema (targets: `merlin_research_manager.py`, `docs/research/*`). |
| `S058` | done | merlin-agent | Added session traceability links (`linked_task_ids`, `planner_artifacts`) with normalization/migration defaults, brief exposure (`linked_task_ids`, resolved `linked_tasks`, `planner_artifacts`), CLI create/batch support, REST + envelope create validation/pass-through, task-manager helper lookups (`get_task`, `list_tasks_by_ids`), and fixture/manager/API/CLI coverage updates. | Link research sessions to task IDs and planner artifacts for traceability (targets: `merlin_tasks.py`, `merlin_research_manager.py`). |
| `S059` | done | merlin-agent | Added planner-fallback telemetry ingestion pipeline: `merlin_quality_gates.ingest_planner_fallback_telemetry(...)` with sink registration + normalized telemetry envelope; `ResearchManager.ingest_planner_fallback_telemetry(...)` maps fallback telemetry to structured signals; API auto-ingests telemetry for `assistant.chat.request` when `include_metadata=true` and `research_session_id` is provided; added quality-gates/research-manager/API tests. | Ingest planner fallback telemetry as structured research signals automatically (targets: `merlin_quality_gates.py`, `merlin_research_manager.py`). |
| `S060` | done | merlin-agent | Added `research packet` CLI command that generates a CP packet markdown skeleton from `get_brief` + session data (default output under `docs/research/CHIMERA_V2_CP_PACKET_SKELETON_<session_id>.md`), plus CLI coverage and template doc `docs/research/CP_PACKET_SKELETON_TEMPLATE.md`. | Add CLI command to generate CP packet skeletons from session briefs (targets: `merlin_cli.py`, `docs/research`). |
| `S061` | done | merlin-agent | Added incremental SHA-256 hashing cache (`resource_hash_cache_path`) to core indexer and resource-indexer plugin: unchanged files reuse cached hashes via `(relative_path,size,mtime_ns)` fingerprints; changed files recompute and refresh cache; indexed entries now include `sha256`; added cache-reuse unit coverage in `tests/test_merlin_resource_indexer.py`. | Add incremental hashing index updates for resource files to reduce full rescans (targets: `merlin_resource_indexer.py`, `plugins/resource_indexer/plugin.py`). |
| `S062` | done | merlin-agent | Refactored watcher with debounce + backpressure queue controls (`MERLIN_WATCH_DEBOUNCE_SECONDS`, `MERLIN_WATCH_MAX_PENDING_EVENTS`, `MERLIN_WATCH_PROCESS_INTERVAL_SECONDS`), bounded pending-event processing, dropped-event accounting, and incremental `index_file` updates on change; added watchdog-missing fallback stubs and new watcher unit coverage. | Add debounce/backpressure controls in file watching to prevent thrash (targets: `merlin_watcher.py`, `merlin_resource_indexer.py`). |
| `S063` | done | merlin-agent | Added retrieval diagnostics in `MerlinRAG`: each `search(...)` now computes/stores `top_k_hit_rate` and `source_diversity` (plus `hit_count`, `k`) and exposes them via `get_last_retrieval_diagnostics()`. Added deterministic diagnostics assertions in RAG tests. | Add retrieval relevance diagnostics (`top_k_hit_rate`, `source_diversity`) for RAG queries (targets: `merlin_rag.py`, `tests/test_merlin_rag.py`). |
| `S064` | done | merlin-agent | Added deterministic citation helpers in routing contract (`deterministic_source_id`, `normalize_rag_citations`) and wired search/RAG operation + REST responses to include normalized `citations` (`source_id`, `path`). Updated routing-contract/API tests and RAG/search expected-response fixtures for deterministic source IDs. | Normalize citation format for RAG responses with deterministic source IDs (targets: `merlin_rag.py`, `merlin_routing_contract.py`). |
| `S066` | done | merlin-agent | Added plugin permission-tier policy (`read/write/network/exec`) in `ExecutionPolicyManager`, manifest permission validation/defaulting in `PluginManager`, execution-time policy enforcement returning `PLUGIN_PERMISSION_DENIED`, and targeted policy/plugin/API tests. | Add plugin permission tiers (`read`, `write`, `network`, `exec`) with policy checks (targets: `merlin_plugin_manager.py`, `merlin_policy.py`). |
| `S067` | done | merlin-agent | Added plugin timeout budgets (`manifest.timeout_seconds`, plugin/default fallbacks), timeout result code (`PLUGIN_TIMEOUT`), cancellation-hook registration/cleanup (`__merlin_task_id`) via `MerlinTaskManager`, and targeted plugin/task/API tests. | Add plugin execution timeout budgets and cancellation hooks (targets: `merlin_plugin_manager.py`, `merlin_tasks.py`). |
| `S068` | done | merlin-agent | Added manifest dependency spec validation (`dependencies[]`), semver compatibility checks (`*`, `==`, `>=`), startup preflight skip-on-incompatibility in packaged plugin load, and `scripts/check_secret_hygiene.py --plugin-dependency-check` diagnostics; covered by plugin-manager and script tests. | Add plugin dependency compatibility checker in startup preflight (targets: `merlin_plugin_manager.py`, `scripts/check_secret_hygiene.py`). |
| `S069` | done | merlin-agent | Added plugin crash-state tracking + capped auto-restart budget (`RestartBudget`), isolation guard (`PLUGIN_CRASH_ISOLATED`) after restart exhaustion, plugin health metadata exposure, and targeted self-healing/plugin/API coverage. | Add plugin crash isolation and auto-restart with capped retries (targets: `merlin_plugin_manager.py`, `merlin_self_healing.py`). |
| `S070` | done | merlin-agent | Added plugin catalog filtering by `capability` and `health_state` for both envelope (`merlin.plugins.list`) and REST (`GET /merlin/plugins`) paths, plus validation and targeted API tests; plugin info now includes capabilities/health metadata. | Add plugin catalog API filters by capability and health state (targets: `merlin_plugin_manager.py`, `merlin_api_server.py`). |
| `S071` | done | merlin-agent | Added vector memory maintenance routines: `compact_memories(...)` (dedupe + optional trim) and `cleanup_stale_vectors(...)` (age-based stale cleanup), with targeted vector-memory tests. | Add vector memory compaction and stale-vector cleanup routine (targets: `merlin_vector_memory.py`, `tests/test_merlin_vector_memory.py`). |
| `S072` | done | merlin-agent | Added vector-memory integrity reporting (`integrity_report`) and `scripts/check_vector_memory_integrity.py` (duplicate/invalid-entry checks with optional compaction), with dedicated script + memory tests. | Add vector-memory integrity checker script for index consistency (targets: `merlin_vector_memory.py`, `scripts/`). |
| `S073` | done | merlin-agent | Added migration utility in `merlin_db.py` with idempotent apply (`run_migrations`), schema tracking/version reads, rollback APIs (`rollback_last_migration`, `rollback_migrations`), and DB snapshot backup/restore support in `merlin_backup.py`; added dedicated DB migration tests. | Add schema migration utility for Merlin DB with rollback support (targets: `merlin_db.py`, `merlin_backup.py`). |
| `S074` | done | merlin-agent | Added SQLite concurrency tuning via settings/env (`MERLIN_SQLITE_JOURNAL_MODE`, `MERLIN_SQLITE_SYNCHRONOUS`, `MERLIN_SQLITE_BUSY_TIMEOUT_MS`, `MERLIN_SQLITE_WAL_AUTOCHECKPOINT`, `MERLIN_SQLITE_CACHE_SIZE_KB`, `MERLIN_SQLITE_TEMP_STORE`) and wired PRAGMA application in `merlin_db._connect`; covered by settings+DB pragma tests. | Tune SQLite pragmas and WAL settings for better concurrent read/write behavior (targets: `merlin_db.py`, `merlin_settings.py`). |
| `S075` | done | merlin-agent | Added namespace-level cache telemetry in `merlin_cache.py` (hits/misses/sets/deletes/evictions + hit-rate, memory eviction tracking) and surfaced cache summary/namespace counts in `merlin_metrics_dashboard.py`; added cache/dashboard tests. | Add cache eviction telemetry and hit-rate metrics per cache namespace (targets: `merlin_cache.py`, `merlin_metrics_dashboard.py`). |
| `S076` | done | merlin-agent | Added backup integrity hashing + manifest workflow (`compute_file_sha256`, `write_backup_integrity_manifest`, `verify_backup_integrity`) and CLI verification command in `merlin_backup.py`; integrated checksum generation/verification and metadata stamping in `merlin_backup_to_drive.py`; added backup integrity tests. | Add backup integrity hash + verify command for archive confidence (targets: `merlin_backup.py`, `merlin_backup_to_drive.py`). |
| `S077` | done | merlin-agent | Added restore smoke-test utilities in `merlin_backup.py` (`restore_backup_archive`, `run_restore_smoke_test`) and exposed backup CLI commands in `merlin_cli.py` (`backup smoke-test`, plus `backup create|verify|restore-db`) with CLI coverage tests. | Add restore smoke test command to validate backup usability (targets: `merlin_backup.py`, `merlin_cli.py`). |
| `S078` | done | merlin-agent | Added user-record schema versioning in `merlin_user_manager.py` (`schema_version`, `USER_SCHEMA_VERSION`) with automatic legacy migration helper (`password_hash` -> `hashed_password`, default role/schema backfill) and updated baseline `merlin_users.json`; added migration tests. | Add schema version field in user profile JSON and migration helper (targets: `merlin_user_manager.py`, `merlin_users.json`). |
| `S079` | done | merlin-agent | Added voice benchmark dataset metadata/provenance support in `merlin_voice_benchmark.py` (`--dataset-version`, `--source-catalog`, `--source-id`, catalog load + metadata builder) and versioned/provenance-rich source catalog in `merlin_voice_sources.json`; added metadata helper tests. | Add voice benchmark dataset versioning and provenance fields (targets: `merlin_voice_benchmark.py`, `merlin_voice_sources.json`). |
| `S080` | done | merlin-agent | Added deterministic TTS fallback metadata capture in `merlin_voice_router.py` (`fallback_reason_code`, attempted/available engines, selected engine) and text-fallback envelope generation in `merlin_voice.py` with deterministic `fallback_id`; also hardened voice module for missing SpeechRecognition dependency and added targeted fallback metadata tests. | Add deterministic fallback-to-text metadata when voice routes fail (targets: `merlin_voice_router.py`, `merlin_voice.py`). |
| `S081` | done | merlin-agent | Added fixture-driven frontend contract generator `scripts/generate_frontend_operation_contracts.py` with `--check` drift mode, generated typed mapping file `frontend/src/services/operationContracts.generated.ts`, and integrated typed contract accessors in `frontend/src/services/api.ts`; added generator tests. | Generate typed frontend client contracts from operation fixtures to prevent drift (targets: `frontend/src/services`, `tests/fixtures/contracts`). |
| `S082` | done | merlin-agent | Added fallback taxonomy summary cards plus sparkline trend rows in both analytics panels using bounded refresh-history sampling. | Add dashboard panel for fallback taxonomy counts and trend lines (targets: `frontend/src/components/AgentAnalytics.tsx`, `frontend/src/components/PluginAnalytics.tsx`). |
| `S083` | done | merlin-agent | Added `ResearchSessions` explorer page (search/tag/cursor/detail/brief) and onboarding service API helpers for research-manager list/search/get/brief flows; wired route/nav entry. | Add research-manager session explorer page in frontend (targets: `frontend/src/pages`, `frontend/src/services/onboarding.ts`). |
| `S084` | done | merlin-agent | Added global command palette (`Ctrl/⌘K`) for operation discovery and quick `POST /merlin/operations` envelope execution with payload editor and response panel. | Add command palette for operation discovery and quick execution (targets: `frontend/src/App.tsx`, `frontend/src/components`). |
| `S085` | done | merlin-agent | Applied narrow-width overflow hardening (`min-w-0`, break/overflow controls, responsive padding/card behavior) across dashboard/layout/model/system/snapshot surfaces. | Run responsive layout audit and fix overflow issues on narrow widths (targets: `frontend/src/components/*.css`, `frontend/src/index.css`). |
| `S086` | done | merlin-agent | Upgraded `check-dist-size.js` with explicit total/file/js/css budgets + JSON report artifact and enforced CI quality-job frontend build gate with report upload. | Add frontend bundle-size budget check in CI (targets: `frontend/scripts/check-dist-size.js`, `.github/workflows/ci.yml`). |
| `S087` | done | merlin-agent | Added Tauri panic hook crash reporter writing local JSON reports and optional opt-in upload path via `tauri.conf.json` plugin config/env overrides. | Add Tauri crash reporting hook with local opt-in upload (targets: `frontend/src-tauri/src/main.rs`, `frontend/src-tauri/tauri.conf.json`). |
| `S088` | done | merlin-agent | Added user-facing retry guidance callouts and retry actions for snapshot/system fallback/error states. | Add user-facing retry guidance for fallback cases in UI responses (targets: `frontend/src/components/SnapshotSummary.tsx`, `frontend/src/components/SystemInfo.tsx`). |
| `S089` | done | merlin-agent | Improved accessibility in core UI: onboarding keyboard navigation + explicit labels/ids/aria hooks, nav `aria-current`, and improved analytics contrast defaults. | Add accessibility pass for keyboard nav, labels, and contrast in core components (targets: `frontend/src/components`, `frontend/src/pages/Onboarding.tsx`). |
| `S090` | done | merlin-agent | Consolidated shared theme tokens into CSS custom properties in `index.css` and aligned component-local styles (agent/system/snapshot/command palette) to those variables. | Consolidate theme variables and remove duplicated style tokens (targets: `frontend/src/index.css`, `frontend/src/components/*.css`). |
| `S091` | done | merlin-agent | Added pre-commit schema sync mode (`--precommit`) in `scripts/sync_contract_schemas.py` and expanded `.pre-commit-config.yaml` with explicit lint gate (`py_compile` on critical Python modules) plus targeted contract test hooks (`test_contract_schemas` + operation response/error suites). | Add pre-commit tasks for schema sync, lint, and targeted contract tests (targets: `.pre-commit-config.yaml`, `scripts/sync_contract_schemas.py`). |
| `S092` | done | merlin-agent | Added phased mypy rollout (`merlin_llm_backends` strict phase + `merlin_api_server` implicit-optional tightening), included backend module in typed file set, and hardened key helper annotations in both modules. | Roll out stricter `mypy` config in phases by module criticality (targets: `mypy.ini`, `merlin_api_server.py`, `merlin_llm_backends.py`). |
| `S094` | done | merlin-agent | Added machine-readable secret hygiene reporting in `scripts/check_secret_hygiene.py` (`--report-json`, `--fail-on`) with fail-policy controls and tests, then wired CI quality job to generate and upload `artifacts/security/secret-hygiene-report.json` while enforcing high-severity failure policy. | Add CI secret-scan policy report artifact and fail conditions (targets: `scripts/check_secret_hygiene.py`, `.github/workflows/ci.yml`). |
| `S095` | done | merlin-agent | Added weekly dependency-audit workflow job in `.github/workflows/ci.yml` (scheduled + manual dispatch) using `pip-audit` with JSON output, markdown report generation, artifact upload, and explicit gate failure on scan errors/vulnerabilities; added report generator script/tests and annotated `requirements.txt` as audit source manifest. | Add dependency vulnerability scan and weekly report generation (targets: `requirements.txt`, `.github/workflows/ci.yml`). |
| `S096` | done | merlin-agent | Added `scripts/run_release_checklist.py` to automate release readiness checks for contracts/protocol docs/test assets with optional command execution and JSON artifact output (`AAS.ReleaseChecklistReport`); updated `docs/AGENT_TRANSITION.md` with strict and deep-checklist invocations; added release checklist script tests. | Add release checklist automation script for artifacts/contracts/tests (targets: `scripts/`, `docs/AGENT_TRANSITION.md`). |
| `S097` | done | merlin-agent | Added tag-based changelog generator module `merlin_changelog.py` with packaging entrypoint (`merlin-changelog` in `setup.py`), documented usage in `README.md`, and wired a CI changelog artifact job for tag/workflow-dispatch runs in `.github/workflows/ci.yml`; added changelog tests. | Add changelog generation pipeline from tagged commits (targets: `setup.py`, `README.md`, `.github/workflows/ci.yml`). |
| `S098` | done | merlin-agent | Added structured benchmark output + CLI in `merlin_benchmark.py` (`AAS.Benchmark.LLMResult`, optional JSON output) and introduced standardized benchmark command pack runner `scripts/run_benchmark_command_pack.py` (dry-run/execute/selective commands, JSON report artifact, critical/non-critical failure policy); added command-pack tests. | Add standardized benchmark command pack for local performance snapshots (targets: `merlin_benchmark.py`, `scripts/`). |
| `S099` | done | merlin-agent | Added subsystem incident templates in `docs/research/INCIDENT_RUNBOOK_TEMPLATES_2026-02-19.md` (API, routing, plugins, research manager) and wired references/usage into `RUNBOOK.md`. | Add incident runbook templates per subsystem (API, routing, plugins, research manager) (targets: `RUNBOOK.md`, `docs/research`). |
| `S100` | done | merlin-agent | Added quarterly architecture drift review checklist + tracked action table in `ARCHITECTURE.md` and synced roadmap-level drift-review cadence/action register in `docs/MERLIN_LONG_TERM_ROADMAP.md`. | Add quarterly architecture drift review checklist with tracked actions (targets: `ARCHITECTURE.md`, `docs/MERLIN_LONG_TERM_ROADMAP.md`). |
| `S101` | done | merlin-agent | Added optional uncertainty-aware DMS escalation gate (`DMS_UNCERTAINTY_ROUTING_ENABLED`, `DMS_UNCERTAINTY_SCORE_THRESHOLD`) in shared `should_prefer_dms_route`, wired adaptive/parallel/streaming routers to pass settings, and added routing/settings/adaptive regression tests covering high-uncertainty preference and long-simple prompt suppression. | Add uncertainty-aware DMS escalation so high-uncertainty reasoning prompts prefer DMS while simple prompts can stay control despite length (targets: `merlin_routing_contract.py`, `merlin_settings.py`, `merlin_*_llm.py`). |
| `S102` | done | merlin-agent | Added streaming router regression corpus suite (short control, long DMS, token-aware DMS, uncertainty-gated suppression, uncertainty-gated escalation) to lock routing-selection parity with adaptive/parallel coverage. | Add streaming router regression corpus with expected model selections and uncertainty-routing parity checks (targets: `tests/test_merlin_streaming_llm.py`). |
| `S103` | done | merlin-agent | Extended parallel router regression corpus with uncertainty-routing parity cases (simple long prompt suppression + uncertain reasoning escalation) and explicitly pinned uncertainty mode defaults for existing corpus scenarios to avoid environment-driven drift. | Add uncertainty-routing regression coverage for parallel router selection parity with adaptive/streaming suites (targets: `tests/test_merlin_parallel_llm.py`). |
| `S104` | done | merlin-agent | Extended adaptive router regression corpus with uncertainty-routing parity scenarios (simple long prompt suppression + uncertain reasoning escalation) and pinned `DMS_UNCERTAINTY_ROUTING_ENABLED` defaults in existing corpus cases to prevent env-driven drift. | Add uncertainty-routing regression coverage for adaptive router selection parity with parallel/streaming suites (targets: `tests/test_merlin_adaptive_llm.py`). |
| `S105` | done | merlin-agent | Added shared routing-regression uncertainty corpus helper module (`routing_regression_corpus.py`) and wired adaptive/parallel/streaming corpus tests to reuse shared long/uncertain query constants plus uncertainty settings builders, reducing cross-suite drift and duplicated literals. | Centralize shared uncertainty-routing regression corpus inputs for adaptive/parallel/streaming suites (targets: `routing_regression_corpus.py`, `tests/test_merlin_*_llm.py`). |
| `S106` | done | merlin-agent | Added routing-contract uncertainty threshold clamp regression tests to lock lower-bound (`<0 -> 0`) and upper-bound (`>1 -> 1`) behavior in `should_prefer_dms_route` when uncertainty mode is enabled. | Add uncertainty-threshold clamp coverage for shared DMS routing contract (targets: `tests/test_merlin_routing_contract.py`). |
| `S107` | done | merlin-agent | Added uncertainty-mode context-interaction tests for shared routing contract: high-context alone remains below threshold, while high-context plus reasoning query crosses threshold and prefers DMS. | Add context+uncertainty interaction coverage for `should_prefer_dms_route` decision boundaries (targets: `tests/test_merlin_routing_contract.py`). |
| `S108` | done | merlin-agent | Added uncertainty-threshold boundary regression tests and fixed floating-point boundary handling in `should_prefer_dms_route` via epsilon compare so exact-threshold uncertainty scores are treated as inclusive while above-threshold checks still reject. | Add threshold-boundary coverage and precision-safe inclusive comparison for shared DMS uncertainty routing decisions (targets: `merlin_routing_contract.py`, `tests/test_merlin_routing_contract.py`). |
| `S109` | done | merlin-agent | Added opt-in sensitive-task DMS guardrail using context task-type allowlist enforcement in shared routing policy, wired adaptive router setting gate, and added regression coverage for blocked disallowed task types and allowed-path behavior. | Add sensitive task-type guardrail layer before DMS routing decisions (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_settings.py`, `tests/test_merlin_routing_contract.py`, `tests/test_merlin_adaptive_llm.py`, `tests/test_merlin_settings.py`). |
| `S110` | done | merlin-agent | Added opt-in adaptive DMS shadow-validation mode that executes DMS in control-variant paths without changing user-facing selection, captures shadow success/latency/quality metadata, and tracks shadow telemetry counters. | Add policy-driven canary/shadow validation path for DMS rollout safety (targets: `merlin_adaptive_llm.py`, `merlin_settings.py`, `tests/test_merlin_adaptive_llm.py`, `tests/test_merlin_settings.py`). |
| `S111` | done | merlin-agent | Added opt-in DMS quality auto-pause budget in adaptive routing using rolling quality-score windows with minimum sample thresholds, cooldown lockout, budget-trip telemetry, and status metadata to block DMS temporarily when quality drops below threshold. | Add DMS quality-first auto-pause controls for safer rollout behavior (targets: `merlin_adaptive_llm.py`, `merlin_settings.py`, `tests/test_merlin_adaptive_llm.py`, `tests/test_merlin_settings.py`). |
| `S112` | done | merlin-agent | Added explicit `router_rule_version` metadata contract field (aliased to policy version), enforced runtime consistency with `router_policy_version`, and updated routing contracts/fixtures/tests to lock schema-backed presence across chat metadata outputs. | Add policy-ledger version tag for routing decisions with contract-backed compatibility (targets: `merlin_routing_contract.py`, `contracts/*.schema.json`, `tests/fixtures/contracts`, `tests/test_merlin_routing_contract.py`, `tests/test_merlin_api_server.py`). |
| `S113` | done | merlin-agent | Added DMS model capability/provenance matrix with non-commercial awareness, waiver/enforcement policy gates, readiness metadata exposure, and fallback blocking behavior when enforcement is active without waiver; expanded backend/settings tests for warn/block/allow paths. | Introduce DMS capability matrix and model provenance checks (including non-commercial model license awareness) (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S114` | done | merlin-agent | Added optional `DMS_REASONING_EFFORT` setting passthrough on DMS chat payloads and extended usage normalization to parse provider `reasoning_tokens` details when present, with focused backend/settings test coverage. | Track `reasoning_effort` and reasoning token fields as a control input when available (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S115` | done | merlin-agent | Added optional DMS connect/read timeout split profile and DMS-specific retry-attempt cap wiring (warmup + chat), while preserving shared retry behavior for non-DMS backends; added backend/settings regression tests for tuple timeout dispatch and capped retry attempts. | Add DMS-specific request timeout profile with connect/read split and low default retry budget (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S116` | done | merlin-agent | Added streaming usage-event normalization/aggregation for OpenAI-compatible SSE payloads (`usage`, token-detail fields, cached/reasoning tokens), threaded normalized usage into routing metadata (`stream_usage_normalized`) for selected streamed responses, and added focused streaming usage tests. | Add streaming usage normalization and decision metadata capture for SSE usage events to track DMS token economics in streamed paths (targets: `merlin_streaming_llm.py`, `tests/test_merlin_streaming_llm.py`). |
| `S117` | done | merlin-agent | Added explicit SSE frame/event parsing for streaming backends (`event` + multiline `data`) with error-event detection (`error`, `*.error`, error payloads), surfaced provider error events as deterministic stream error markers, and added DMS early-stream failover (`dms_stream` stage) when first chunk is an error marker so control fallback can continue cleanly; added focused frame/error/failover tests. | Add streaming error-event grammar hardening so SSE error frames are classified and routed through deterministic fallback behavior (targets: `merlin_streaming_llm.py`, `tests/test_merlin_streaming_llm.py`). |
| `S118` | done | merlin-agent | Added adaptive-router usage economics tracking for DMS experiments: normalized OpenAI-compatible usage extraction in `_call_model`, selected-response usage stamping in metadata (`usage_normalized`), prompt-bucket usage rollups in routing metrics, and shadow-validation usage delta capture (`dms_shadow_usage_delta`) with aggregate delta counters; added focused adaptive usage tests. | Track usage-token deltas and prompt-bucket economics for DMS experiment paths using normalized `usage`/`cached_tokens` fields (targets: `merlin_adaptive_llm.py`, `tests/test_merlin_adaptive_llm.py`). |
| `S119` | done | merlin-agent | Added parallel-router usage economics tracking for DMS experiment flows: normalized OpenAI-compatible usage extraction in `_call_model`, selected-response usage metadata stamping (`usage_normalized`), and routing-metric rollups by prompt bucket and A/B variant (`dms`/`control`/`disabled`); added focused parallel usage tests and reset hooks for usage-economics metrics. | Extend token-economics instrumentation to parallel routing so DMS/control experiment usage can be compared by prompt bucket and A/B variant (targets: `merlin_parallel_llm.py`, `tests/test_merlin_parallel_llm.py`, `tests/conftest.py`). |
| `S120` | done | merlin-agent | Added streaming-router usage economics tracking for DMS experiment flows: selected-stream usage metadata normalization (`stream_usage_normalized`), routing-metric rollups by prompt bucket and A/B variant (`dms`/`control`/`disabled`), and focused streaming assertions for DMS/control/disabled cohorts. | Extend token-economics instrumentation to streaming routing so DMS/control experiment usage can be compared by prompt bucket and A/B variant (targets: `merlin_streaming_llm.py`, `tests/test_merlin_streaming_llm.py`). |
| `S121` | done | merlin-agent | Added status-level streaming usage economics summarization with per-sample averages (`selected_avg_*`) and per-group averages for prompt buckets and AB variants, then asserted avg fields in focused streaming DMS/control tests. | Add usage-economics average rollups in streaming routing status so token-cost proxies are directly consumable by telemetry dashboards (targets: `merlin_streaming_llm.py`, `tests/test_merlin_streaming_llm.py`). |
| `S122` | done | merlin-agent | Added status-level usage economics summarization in adaptive + parallel routers with per-sample averages (`selected_avg_*`), grouped averages (`by_prompt_bucket`, and `by_ab_variant` for parallel), and adaptive shadow-delta averages; added focused assertions in adaptive/parallel usage-economics tests. | Add usage-economics average rollups to adaptive and parallel routing status payloads for parity with streaming telemetry consumption (targets: `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `tests/test_merlin_adaptive_llm.py`, `tests/test_merlin_parallel_llm.py`). |
| `S123` | done | merlin-agent | Extended metrics dashboard summary telemetry to surface routing usage-economics averages from adaptive/parallel/streaming backends (selected avg total tokens, DMS cohort avg total tokens, and adaptive shadow delta avg total tokens), with focused dashboard status test coverage. | Surface usage-economics average rollups in dashboard summary for routing telemetry consumption parity across adaptive/parallel/streaming paths (targets: `merlin_metrics_dashboard.py`, `tests/test_merlin_metrics_dashboard.py`). |
| `S124` | done | merlin-agent | Added optional DMS prompt-cache hint controls (`DMS_PROMPT_CACHE_KEY_ENABLED`, `DMS_PROMPT_CACHE_KEY_PREFIX`) and deterministic `prompt_cache_key` payload stamping in `_dms_chat`, with focused backend/settings tests. | Add optional DMS request-level prompt cache key hints for repeated-context workloads while keeping default behavior unchanged (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S125` | done | merlin-agent | Added optional DMS trace-header controls (`DMS_TRACE_HEADER_ENABLED`, `DMS_TRACE_HEADER_NAME`) and per-request trace header propagation on DMS outbound chat calls, with focused backend/settings coverage. | Add request-scoped trace header propagation for DMS calls to improve end-to-end incident correlation while preserving default behavior (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S126` | done | merlin-agent | Added SSE parser contract tests for malformed frame tolerance (`_iter_sse_frames` ignores unknown/colonless lines) and streaming-path continuation when malformed lines appear before valid chunks. | Add SSE parser contract tests for malformed frames so streaming ingestion remains deterministic under noisy provider/event streams (targets: `tests/test_merlin_streaming_llm.py`). |
| `S127` | done | merlin-agent | Added optional DMS prompt-bucket timeout settings (`DMS_PROMPT_BUCKET_TIMEOUTS_ENABLED`, `DMS_TIMEOUT_SHORT_S`, `DMS_TIMEOUT_MEDIUM_S`, `DMS_TIMEOUT_LONG_S`) and wired DMS timeout selection by prompt bucket when enabled while preserving explicit-timeout precedence; added backend/settings tests. | Add DMS timeout budgets per prompt cohort so short/medium/long requests can use tuned timeout envelopes without changing default behavior (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S128` | done | merlin-agent | Added optional DMS request-cap controls (`DMS_REQUEST_RATE_LIMIT_ENABLED`, `DMS_REQUEST_RATE_LIMIT_PER_MINUTE`) with backend-side per-minute cap enforcement and deterministic LM Studio fallback when exceeded; added backend/settings tests. | Add rate-limit-aware DMS request caps to reduce bursty DMS overload risk while preserving default routing behavior (targets: `merlin_llm_backends.py`, `merlin_settings.py`, `tests/test_merlin_llm_backends.py`, `tests/test_merlin_settings.py`). |
| `S129` | done | merlin-agent | Added streaming ingestion contract tests for SSE edge cases: non-JSON `data:` payloads are skipped without aborting stream assembly, and terminal usage-only chunks (no `choices`) still contribute normalized usage totals. | Strengthen deterministic streaming ingestion guarantees for noisy OpenAI-compatible SSE implementations (`data` anomalies + usage-only terminal events) (targets: `tests/test_merlin_streaming_llm.py`). |
| `S130` | done | merlin-agent | Added deterministic routing trace metadata contract fields (`routing_trace_id`, `routing_trace_fingerprint`, `routing_trace_scheme`) in shared routing decisions, threaded query-seeded trace generation through adaptive/parallel/streaming and emotion-chat fallback paths, synced standalone + embedded operation-envelope routing schemas, and expanded routing/backend contract assertions. | Include trace metadata for routing decisions to improve postmortem correlation and deterministic replay of router choices (targets: `merlin_routing_contract.py`, `merlin_*_llm.py`, `contracts/*.schema.json`, `tests/test_merlin_routing_contract.py`, `tests/test_merlin_*_llm.py`). |
| `S131` | done | merlin-agent | Added shared `enrich_error_with_rate_limit_headers(...)` utility to append observed OpenAI-style `x-ratelimit-*`/`retry-after` header snapshots to HTTP error detail, then wired adaptive/parallel/streaming model-call error paths to preserve those details in fallback surfaces; added focused routing/adaptive/parallel/streaming tests. | Capture rate-limit header context on DMS failures so fallback reason details carry quota/reset signals for faster incident triage (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `merlin_streaming_llm.py`, `tests/test_merlin_*`). |
| `S132` | done | merlin-agent | Added maturity-tier runtime settings (`MERLIN_MATURITY_TIER`, `MERLIN_MATURITY_POLICY_VERSION`) and stamped them into routing metadata + operation-envelope response metadata; synced routing schemas/fixtures, added operation middleware body-replay compatibility for stable `/merlin/operations` parsing under installed dependency set, and expanded settings/routing/API tests. | Settings + metadata fields for maturity tier stamping (targets: `merlin_settings.py`, `merlin_routing_contract.py`, `merlin_api_server.py`, `contracts/*.schema.json`, `tests/*`). |
| `S133` | done | merlin-agent | Added per-tier maturity operation allowlist settings (`MERLIN_MATURITY_OPERATION_ALLOWLISTS`) with wildcard defaults and enforced API dispatch gate returning `OPERATION_NOT_ALLOWED_FOR_MATURITY_TIER` when current tier disallows an operation; added focused settings/API coverage and env documentation. | Add per-tier operation allowlist gate in API dispatch (targets: `merlin_api_server.py`, `merlin_settings.py`, `tests/test_merlin_api_server.py`). |
| `S134` | done | merlin-agent | Added high-risk operation class policy hooks in `merlin_policy.py` with tier-aware mentor-pass requirements (`MERLIN_MENTOR_PASS_REQUIRED_TIERS`), enforced envelope-time mentor-pass checks in API validation (`OPERATION_REQUIRES_MENTOR_PASS`), and added focused policy/API regression tests for block and allow paths. | Add mentor-pass policy hook for high-risk operation classes (targets: `merlin_policy.py`, `merlin_api_server.py`, `tests/test_merlin_policy.py`, `tests/test_merlin_api_server.py`). |
| `S135` | done | merlin-agent | Added `scripts/evaluate_maturity_promotion.py` to compute promotion/demotion recommendations from release/smoke evidence and policy/regression/error-budget thresholds, emit `AAS.MaturityPromotionDemotionReport` JSON artifacts, and support strict demotion exits; added dedicated subprocess tests for promote/demote/hold paths. | Add promotion/demotion evaluator script + JSON report artifact (targets: `scripts/`, `tests/test_run_release_checklist.py` or dedicated evaluator tests). |
| `S136` | done | merlin-agent | Added memory-confidence lifecycle in `merlin_research_manager.py`: hypothesis updates now apply time-decay weighting (`timestamp_utc` age) plus duplicate-evidence reinforcement (`duplicate_count`) with derived memory strength fields, duplicate-signal path now re-scores hypotheses, and briefs re-evaluate hypotheses/foresight at read time; added focused reinforcement and stale-signal decay tests. | Add memory-confidence decay/reinforcement lifecycle in research manager (targets: `merlin_research_manager.py`, `tests/test_merlin_research_manager.py`). |
| `S137` | done | merlin-agent | Added `scripts/scaffold_incident_regression.py` to generate incident-driven regression scaffolds (fixture JSON + skipped pytest template) with dry-run and overwrite controls, enabling a repeatable incident-to-regression pipeline; added dedicated script tests for create/overwrite-guard/dry-run behavior. | Add incident-to-regression scaffold command for turning incidents into fixture scaffolds (targets: `scripts/`, `tests/`). |
| `S138` | done | merlin-agent | Added maturity dashboard status payload in `merlin_metrics_dashboard.py` (tier, policy version, readiness, regression status, recommendation, gate counts) sourced from runtime settings + latest evaluator artifact, surfaced summary passthrough fields, added backend coverage in `tests/test_merlin_metrics_dashboard.py`, and integrated a frontend `MaturityStatusCard` component into `Dashboard.tsx`. | Add maturity dashboard status card showing tier/readiness/regression status (targets: `merlin_metrics_dashboard.py`, `frontend/src/components`, `tests/test_merlin_metrics_dashboard.py`). |
| `S139` | done | merlin-agent | Extended `scripts/run_release_checklist.py` optional command pack to invoke `scripts/evaluate_maturity_promotion.py` and emit a sibling maturity evaluator artifact (optionally seeded with latest release/smoke reports); added release-checklist test coverage asserting evaluator command inclusion and artifact schema output. Validation pass also restored `/merlin/operations` middleware request-body replay in `merlin_api_server.py` to prevent consumed-body hangs under current Starlette behavior. | Integrate maturity promotion evaluator into release-checklist optional validation commands and keep operation middleware body replay compatibility for contract-suite stability (targets: `scripts/run_release_checklist.py`, `merlin_api_server.py`, `tests/test_run_release_checklist.py`). |
| `S140` | done | merlin-agent | Routed emotion-chat prompt bucketing through shared routing-contract helpers (`prompt_size_bucket`, token-aware thresholds) and replaced generic error fallback stamping with centralized `apply_dms_fallback(...)`; aligned disabled-DMS reason code to `RoutingFallbackReasonCode` enum and added focused emotion-chat metadata regressions. | Deduplicate emotion-chat routing policy fragments and fallback classification to reduce drift with adaptive/parallel/streaming paths (targets: `merlin_emotion_chat.py`, `tests/test_merlin_emotion_chat.py`). |
| `S141` | done | merlin-agent | Added shared query-bucket resolver (`resolve_query_prompt_bucket`) in routing contract and switched adaptive/parallel/streaming/emotion-chat callers to consume it, removing duplicated token-aware prompt-bucketing snippets while preserving existing DMS routing behavior; added routing-contract regression coverage for bucket + token return semantics. | Deduplicate prompt-bucket/token-estimation glue across router backends to reduce policy-drift risk (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `merlin_streaming_llm.py`, `merlin_emotion_chat.py`). |
| `S142` | done | merlin-agent | Added shared task-type allowlist normalizer (`normalize_task_type_allowlist`) and switched adaptive/parallel/streaming routing gates to consume it for both reasoning checks and DMS preference decisions, removing repeated lowercase/set-comprehension snippets while preserving routing behavior; added routing-contract regression coverage for normalization/filter semantics. | Deduplicate task-type allowlist normalization across router backends to reduce policy-drift risk (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `merlin_streaming_llm.py`, `tests/test_merlin_routing_contract.py`). |
| `S143` | done | merlin-agent | Added shared settings-driven DMS route gate helper (`should_prefer_dms_route_from_settings`) that encapsulates prompt-token resolution, task-type allowlist normalization, and uncertainty/threshold wiring from runtime settings; switched adaptive/parallel/streaming `_should_prefer_dms` call sites to use the helper and added routing-contract regression coverage for context-allowlist enforcement behavior. | Deduplicate settings-to-route-gate wiring across router backends to reduce policy-drift risk (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `merlin_streaming_llm.py`, `tests/test_merlin_routing_contract.py`). |
| `S144` | done | merlin-agent | Removed now-unused `_is_reasoning_query` helper methods from parallel/streaming backends and cleaned stale imports after route-gate centralization, reducing dead code paths without changing runtime routing behavior; validated via existing parallel/streaming regression suites. | Prune dead route-gate helper fragments left after settings-driven centralization to reduce maintenance drift (targets: `merlin_parallel_llm.py`, `merlin_streaming_llm.py`, `tests/test_merlin_parallel_llm.py`, `tests/test_merlin_streaming_llm.py`). |
| `S145` | done | merlin-agent | Added shared usage helpers (`as_non_negative_int`, `normalize_openai_usage_payload`, `coerce_usage_normalized`) in routing contract and rewired adaptive/parallel/streaming usage normalization/coercion call sites to consume them, reducing duplicate token-usage parsing logic while preserving behavior; added routing-contract regression coverage for normalization/coercion edge cases. | Deduplicate OpenAI-compatible usage normalization/coercion glue across adaptive/parallel/streaming routers to reduce drift risk in DMS token-economics telemetry (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `merlin_streaming_llm.py`, `tests/test_merlin_routing_contract.py`). |
| `S146` | done | merlin-agent | Added shared content extraction helper (`extract_openai_compatible_content`) in routing contract and rewired adaptive/parallel `_extract_openai_compatible_content` wrappers to consume it, removing duplicated payload-shape parsing logic while preserving existing response extraction semantics; added routing-contract coverage for supported payload shapes and scalar fallback behavior. | Deduplicate OpenAI-compatible content extraction glue across adaptive/parallel routers to reduce parser drift risk in model response handling (targets: `merlin_routing_contract.py`, `merlin_adaptive_llm.py`, `merlin_parallel_llm.py`, `tests/test_merlin_routing_contract.py`). |
| `S147` | done | merlin-agent | Rewired `LLMBackend._extract_openai_compatible_content` to consume the shared routing-contract helper (`extract_openai_compatible_content`), removing the remaining duplicated OpenAI-compatible response-content parser implementation while preserving existing extraction behavior covered by backend tests. | Complete deduplication of OpenAI-compatible content extraction across LLM backend modules to reduce future parser drift (targets: `merlin_llm_backends.py`, `merlin_routing_contract.py`, `tests/test_merlin_llm_backends.py`). |
| `S148` | done | merlin-agent | Expanded shared usage normalization helper (`normalize_openai_usage_payload`) with optional top-level fallback field support and zero-field behavior controls, then rewired `LLMBackend._normalize_usage` to consume it (including `prompt_eval_count`/`eval_count` compatibility and zero-default usage maps); added routing-contract regression tests for optional fallback/zero-field modes and removed the now-unused local integer coercion helper from `merlin_llm_backends.py`. | Complete deduplication of OpenAI-compatible usage normalization across LLM backend modules while preserving provider-alt usage compatibility and default usage-shape guarantees (targets: `merlin_routing_contract.py`, `merlin_llm_backends.py`, `tests/test_merlin_routing_contract.py`, `tests/test_merlin_llm_backends.py`). |
| `S149` | done | merlin-agent | Added explicit request-body replay helper (`_install_request_body_replay`) in API middleware to restore cross-version Starlette compatibility when operation middleware pre-reads `Request.body()`, preventing downstream `/merlin/operations` deadlocks under older system Python dependency stacks; added direct replay helper regression tests. Verified on 2026-02-23 with required system-`python3` contract suite command (`190 passed`, no hang). | Harden operation middleware request-body replay compatibility across Starlette/FastAPI version variance to eliminate system-`python3` contract-suite hangs (targets: `merlin_api_server.py`, `tests/test_merlin_api_server.py`, `tests/test_operation_expected_responses.py`). |
